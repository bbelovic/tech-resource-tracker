# Observable Void Success

The learner correctly explained that `Observable<void>` means the response body should not be used because there is no typed value such as `id` to inspect. Future lessons can build on this distinction between success-by-body, such as create returning a resource, and success-by-completion, such as update returning HTTP 204.
