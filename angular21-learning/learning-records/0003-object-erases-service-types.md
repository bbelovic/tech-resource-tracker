# Object Erases Service Types

The learner correctly explained that `Observable<Object>` in a component erases the concrete type information gained from a typed service method. Future lessons can now assume the learner understands that types should be preserved across service and component boundaries, not only at the HTTP call site.
