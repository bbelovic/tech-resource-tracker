# Nonnullable Typed Forms

The learner applied `NonNullableFormBuilder`, switched form reading to `getRawValue()`, and chose a valid default resource type instead of an empty string. Future lessons can assume the learner understands the distinction between non-null form values and domain-valid values.
