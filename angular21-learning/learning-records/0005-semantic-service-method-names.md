# Semantic Service Method Names

The learner completed a small API naming refactor from historical names like `getTechResourceById2` and `postNewTechResource2` to semantic names like `getTechResourceById` and `createTechResource`. Future lessons can assume the learner understands that service method names should describe intent and domain behavior, not HTTP mechanics or migration history.
