# Starting Point

The learner has basic TypeScript and Angular familiarity and wants to catch up by refactoring the existing `tech-resource-tracker` frontend. Future lessons should assume syntax is somewhat familiar, but should still explain Angular-specific patterns and TypeScript type design carefully.
