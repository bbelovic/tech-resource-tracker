# Service Names Should Match Shape

The learner correctly identified that `getTechResource()` is misleading when it returns a collection of detail DTOs. Future lessons can assume the learner understands that service method names should communicate both cardinality and response shape, not rely on TypeScript annotations alone.
