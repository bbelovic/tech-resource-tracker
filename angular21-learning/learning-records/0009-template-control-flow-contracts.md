# Template Control Flow Contracts

The learner migrated an `ngSwitch` block to Angular built-in `@switch` while preserving visible text and test attributes. Future lessons can assume the learner understands that template syntax migrations should be separated from DOM, UX, and test-contract changes unless those changes are intentional.
